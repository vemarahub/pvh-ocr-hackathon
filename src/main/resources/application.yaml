---
spring:
  profiles: default
  application:
    name: ReviewApplication
  data:
    mongodb:
      dbName: hackathon
      host: localhost
      port: 27017

server:
  port: 8080

logging:
  level:
    root: INFO
